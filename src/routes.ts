// Add new routes here - sitemap will be automatically updated.
// "/" is included automatically by vite-plugin-sitemap, so omit it here.
export const routes: string[] = [
  "/properties",
  "/photos",
  "/contact",
];
